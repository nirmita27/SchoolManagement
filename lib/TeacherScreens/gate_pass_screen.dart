import 'package:flutter/material.dart';

class GatePassScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gate Pass'),
      ),
      body: Center(
        child: Text('Gate Pass Screen'),
      ),
    );
  }
}
