import 'package:flutter/material.dart';

class FeeDetailsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Fee Details'),
      ),
      body: Center(
        child: Text('Fee Details Screen'),
      ),
    );
  }
}
